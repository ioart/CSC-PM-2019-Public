language: python

python:
  - "3.6"

services:
  - docker

before_install:
  - docker-compose -f task11.1/docker-compose.yml up -d

install:
  - pip install -r requirements.txt

script:
  - cd task11.1
  - pytest
